spring.application.name: employee-svc

server:
  port: 8081
  servlet:
    context-path: /tla/emplSvc

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
logging:
  level:
    org.springframework.web: trace

